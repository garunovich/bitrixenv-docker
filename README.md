# Bitrix Environment 7.2

Build and start container:

> docker build -t bitrixenv .

> docker run -ti -p 80:80 -v /var/www/:/home/bitrix/www garunovich/bitrixenv

> Open in browser: http://localhost/
